@model IEnumerable<backend.Models.TipoFormacao>
@{
    ViewBag.Title = "Buscar tipo de formação";
}
<div class="row my-3">
    <div class="col-md">
        <h1>Listando todos os cursos</h1>
        <h4>Você pode utilizar a busca para filtrar por nome</h4>
        @if (@TempData["alertInfo"] != null) {
            <div class="alert alert-info" role="alert">
                @TempData["alertInfo"]
            </div>
        }
        @if (@TempData["alertSucesso"] != null) {
            <div class="alert alert-success" role="alert">
                @TempData["alertSucesso"]
            </div>
        }
        @if (@TempData["alertErro"] != null) {
            <div class="alert alert-danger" role="alert">
                @TempData["alertErro"]
            </div>
        }
    </div>
</div>
<div class="row mb-4">
    <div class="col-md">
        @Html.ActionLink("Cadastrar Novo Tipo de Formação", "Cadastrar", "TipoFormacao", new { area = "" }, new { @class = "btn btn-success" })
    </div>
</div>
<div class="row">
    <div class="col-md">
        <form action="/TipoFormacao/Buscar" method="get">
            <div class="input-group mb-3">
                <input name="nome" type="text" class="form-control" placeholder="Ex: Graduação" aria-label="Ex: Graduação" aria-describedby="button-addon2">
                <button class="btn btn-outline-secondary" type="submit" id="button-addon2">Buscar</button>
            </div>
        </form>
    </div>
</div>
<div class="row">
    <div class="col-md">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">@Html.DisplayNameFor(model => model.Id)</th>
                    <th scope="col">@Html.DisplayNameFor(model => model.Nome)</th>
                    <th scope="col" width="200">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var tipoFormacao in Model) {
                    <tr>
                        <th scope="row">@Html.DisplayFor(id => tipoFormacao.Id)</th>
                        <td>@Html.DisplayFor(nome => tipoFormacao.Nome)</td>
                        <td>
                            @Html.ActionLink(" ", "Editar", "TipoFormacao", new { id = tipoFormacao.Id }, new { @class = "btn btn-warning far fa-edit" })
                            @Html.ActionLink(" ", "Apagar", "TipoFormacao", new { id = tipoFormacao.Id }, new { @class = "btn btn-danger far fa-trash-alt" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

